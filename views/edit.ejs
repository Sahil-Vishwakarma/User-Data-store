<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
        <link rel="stylesheet" href=/stylesheets/styles.css">
        <script
            src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    </head>
    <body>
        <div class="bg-zinc-900 w-full min-h-screen text-white p-10">
            <a class="text-2xl text-zinc-500" href="/read">Read all Users</a>
            <h1 class="text-2xl text-zinc-500">Update User</h1>
            <form class="mt-5" action="/update/<%=user.id%>" method="post">
                <input
                    class="outline-none bg-zinc-800 rounded-md px-3 py-2 mr-3"
                    placeholder="name"
                    name="name"
                    value="<%=user.name%>">
                <input
                    class="outline-none bg-zinc-800 rounded-md px-3 py-2 mr-3"
                    placeholder="email"
                    name="email"
                    value="<%=user.email%>">
                <input
                    class="outline-none bg-zinc-800 rounded-md px-3 py-2 mr-3"
                    placeholder="image"
                    name="image"
                    value="<%=user.image%>">
                <input
                    class="outline-none text-white bg-yellow-600 px-4 py-2 rounded-md text-black"
                    value="Update User"
                    type="submit">
            </form>
        </div>
    </body>
</html>